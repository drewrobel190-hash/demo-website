/* ============================================================
   Supabase configuration — PUBLIC values only.
   The project URL and the publishable/anon key are safe to expose in a static
   site: your data is protected by Row Level Security, not by hiding the key.
   NEVER put the service_role / secret key here (it bypasses RLS).
   ============================================================ */
window.SUPABASE_CONFIG = {
  url: 'https://bdchmippxtannjrcgkru.supabase.co',
  anonKey: 'sb_publishable_lPYjM-AD7HohSO_CEtJF9w_bG0-JkI1',
  vehicleBucket: 'vehicle-images'
};
